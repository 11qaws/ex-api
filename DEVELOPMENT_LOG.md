# DEVELOPMENT LOG

## 2026-07-27

- `11qaws/ex-api` 독립 저장소와 로컬 격리 작업공간을 초기화했다.
- `rettohighlight`의 `uf-row`에서 왼쪽 정체성 레일, 유레카 테마 틴트, 오른쪽 초상
  bleed와 scrim 구조를 가져왔다.
- 초기 디자인 시안의 핵심인 현재 팔로워와 링피트 결과의 즉시 비교, 차가운 초록과
  뜨거운 주황의 대비를 유지했다.
- 기준 1,031명, 팔로워 1명당 0.5분 계산 모듈과 URL 설정을 구현했다.
- 공식 치지직 Open API의 비밀키를 숨기는 Cloudflare Worker 프록시를 구현했다.
- GitHub Pages 배포 워크플로와 운영 설정 문서를 추가했다.
- 1280×100 실제 렌더에서 초상 과확대를 발견하고, 이미지 크기와 눈 중심 초점을
  와이드 위젯 비율에 맞게 조정했다.
- 정갈한 UI 인상을 줄이고 스트리머의 투박한 표현 취향을 살리기 위해 제목·숫자를
  `Gasoek One`으로 교체하고, 비뚤어진 스탬프·잉크 망점·거친 티켓 그림자를 더했다.
- 컬러 초상을 흰색 러프 선화 자산으로 변환해 오른쪽 초록 배경에 스크린 프린트처럼
  배치했다. 중앙 메시지는 현황 설명 대신 `팔로우 누르면 +30초!` 행동 유도로 바꿨다.
- 반듯한 디스플레이체를 Nintendo DS풍 픽셀 글꼴 `Galmuri11 Bold`로 교체했다.
  떠 있던 결과 티켓은 전체 높이의 고정 주황 패널로 바꾸고 카피를 `도망못감;;`,
  `지금 +n명 쌓임`처럼 방송 채팅에 가까운 말투로 다듬었다.
- 폰트 교체만 반복하던 카드 구성을 폐기하고, 급하게 써 붙인 낙서 전단 콘셉트로
  전체 표현을 다시 설계했다. `팔로우 눌러서 나 운동시키기`를 주 문장으로 올리고
  `1명 = +30초 → 일요일 링피트 n분째ㅋㅋ`를 한 줄 결과 문장으로 통합했다.
- 최종 카피를 `팔로우 눌러서 일요일 링피트 적립 n분 (+30초)` 한 문장으로
  압축했다. `ㄱㄱ`, 참여 인원, 화살표, 부가 낙서를 삭제하고 왼쪽 기준 정보까지
  `Gaegu` 손글씨로 통일했다.
- 방송 화면에서 더 거칠고 즉각적으로 읽히도록 중앙 문장을 40px대로 키우고,
  결과 숫자는 1.3배로 돌출시켰다. 왼쪽 팔로워 메모도 모든 글자 크기를 함께 올렸다.
- 캐릭터 이미지와 수동 LIVE 버튼을 제거하고 동적 요소를 세 가지로 제한했다:
  API 팔로워 수, 기준일부터의 총 링피트 시간, 직전 조회 대비 신규 팔로워 이벤트.
  신규 인원은 30초 단위로 환산해 `+30초`, `+1분`, `+1분 30초`처럼 6초간 표시한다.
- LIVE 버튼 마크업 삭제 뒤 남은 `statusButton` 갱신 코드가 브라우저 실행을
  중단시키는 문제를 렌더 검수에서 발견했다. 잔여 참조를 제거하고 재검증했다.
- OBS 환경별 크기 조절을 위해 `width`, `height`, `fontSize` URL 설정을 추가했다.
  기본값은 1280×100·54px이며, 글자 크기에 맞춰 팔로워 영역과 신규 적립 이벤트
  폭도 함께 비례하도록 CSS 변수로 연결했다.
- 기본 OBS 캔버스를 800×150·48px로 변경했다. 좁아진 가로 폭에서도 세 동적
  요소가 유지되도록 소형 화면 보정 breakpoint를 640px 아래로 내렸다.
- 150px 높이가 방송 패널로는 과도해 기본 캔버스를 800×100으로 다시 낮췄다.
- 800×100에서 증가 이벤트와 총 적립 시간이 겹치지 않도록 문장을
  `팔로우 눌러서 일요일 / 링피트 적립 n분` 두 줄로 재배치했다.
- 800px 폭에서 `n분 30초`가 잘리지 않도록 문장을
  `팔로우 눌러서 일요일 링피트 / 적립 n분 30초`로 분할했다. 조건부 폰트 축소는
  제거하고 총 적립값의 큰 크기를 유지했다.
- 팔로워 증가 시 숫자가 위아래로 교대되고, 오른쪽 적립 스티커가 충돌하듯 들어온
  뒤 총 시간이 형광펜으로 다시 칠해지는 3단계 애니메이션을 연결했다. 평상시에는
  상태 점만 약하게 호흡하고, `prefers-reduced-motion`에서는 이동 효과를 제거한다.
- 애니메이션 종료 후 교체 전 팔로워 숫자 레이어가 다시 나타나 현재 숫자와 겹치던
  문제를 퇴장 레이어의 기본 투명 상태로 수정했다.
- 최초 배포 환경에 치지직 개발자 키가 없는 경우에도 실시간 조회가 가능하도록
  Worker에 치지직 공개 채널 응답 경로를 추가했다. `Client ID`와 `Client Secret`이
  모두 설정되면 기존 공식 Open API 경로를 자동으로 우선한다.
- 팔로워 교체 애니메이션이 `지금 팔로워` 라벨을 침범하지 않도록 숫자 전용
  클리핑 높이와 위아래 간격을 분리했다. 오른쪽 이벤트의 `방금 적립` 라벨은
  약 15px에서 20px로 키우고 패딩을 재배분해 100px 높이에서도 읽히게 했다.
- 외곽 폼, 왼쪽 정체성 띠, 팔로워 구역, 주황 이벤트 폼의 치수와 좌표를
  `docs/WIDGET_FORM_SPEC.md`에 고정했다. 반복 사용되는 치수는 CSS 변수로 추출해
  문서와 실제 구현이 같은 값을 참조하도록 정리했다.

## 2026-07-28

- 기존 OBS 위젯 주소의 하위 호환성을 유지하면서 `/editor/`에 전용 편집기를
  추가했다. 문구, 800×100 기준 폼 크기, 전체·위치별 글자 크기를 입력 즉시
  미리보기 iframe과 URL에 반영한다.
- 편집 상태를 `draft`, `previewUrl`, `widgetUrl`, `copyState`로 분리했다.
  애니메이션 확인용 `preview`, `eventDelta` 값이 실제 OBS 링크에 섞이지 않도록
  별도 URL을 생성한다.
- 전체 글자 크기를 움직이면 기존 디자인 비율에 맞춰 위치별 크기가 함께 바뀌며,
  고급 설정에서 각 위치를 다시 독립 조절할 수 있게 했다.
- 사용자 문구는 공백을 정규화하고 길이를 제한한 뒤 `textContent`로만 출력한다.
  `{initial}` 토큰은 포맷된 기준 팔로워 수로 안전하게 치환한다.
- 편집기 배색은 유레카 초록·주황으로 고정하고, 기본 사용 흐름 뒤에 위치별 폰트와
  계산 설정을 접어 두어 첫 사용자가 문구 수정부터 링크 복사까지 한 화면에서
  끝낼 수 있게 구성했다.
- 팔로워 계산 설정에서 새로고침 입력을 제거했다. 위젯의 기본 30초 갱신은 그대로
  유지하며, 편집기가 만드는 링크에도 불필요한 `refresh` 파라미터를 넣지 않는다.
- 편집기 안의 라이브 미리보기만 10초마다 갱신하도록 미리보기 URL에
  `refresh=10`을 적용했다. OBS용 생성 링크의 기본 30초 주기와는 분리된다.
- 도움말이 있는 기준 안내 필드가 같은 행의 다른 입력칸을 아래로 밀던 문제를
  수정했다. 모든 문구 필드를 위쪽 기준으로 고정해 라벨과 입력창 시작선을 맞췄다.
- `agent/format-accrued-time-hours` 브랜치의 시간 단위 표기와 작은 증가량 배지를
  점검했다. 별도 CSS가 실제 번들에 포함되지 않던 누락을 연결하고, 이벤트 기본
  문구를 편집기·위젯·문서에서 `방금 추가`로 통일했다.
- 누적 시간을 시간·분·초 단위로 분리하고 직전 조회 값과 비교한다. 값이 같은
  단위는 일반 잉크로 유지하고, 숫자가 달라지거나 새로 나타난 연속 단위만 하나의
  주황 형광펜으로 묶어 다시 그리도록 변경했다.
- 미리보기는 현재값을 바로 출력하지 않고 `현재값 - eventDelta`를 먼저 그린 뒤
  650ms 후 현재값으로 전환해 실제 팔로워 증가 애니메이션을 재현한다.
- 하이라이트 비교 단위를 `시간`과 `분·초` 두 묶음으로 조정했다. 초만 바뀌어도
  현재 분·초 전체를 하나의 주황 선으로 다시 그리고, 시간은 정각을 넘어 숫자가
  바뀔 때만 별도의 주황 선으로 갱신한다.
- 최초 API 조회에는 비교 대상이 없으므로 어떤 시간 단위에도 형광펜을 표시하지
  않는다. 이후 조회에서 시간 숫자가 실제로 바뀐 경우에만 시간 단위를 강조한다.
- 시간 숫자가 바뀐 조회에서는 현재 시간·분·초 전체를 하나의 형광펜 묶음으로
  강조한다. 시간이 그대로인 조회에서만 분·초 묶음을 독립적으로 강조한다.
- 긴 시간 표기와 작은 증가량 배지를 함께 검증한 결과를 반영해 기본 OBS 캔버스를
  `650×100`으로 줄였다. 편집기 기본값과 재사용 폼 명세도 같은 규격으로 맞췄다.
- 변화가 없는 다음 조회에서 주황 형광펜을 즉시 제거하지 않고, 왼쪽부터 오른쪽으로
  걷히듯 접은 뒤 일반 시간 표기로 돌아가는 퇴장 애니메이션을 추가했다.
- 형광펜 퇴장 중 변형된 배경 레이어가 시간 글자를 덮어 글자가 먼저 사라져 보이던
  문제를 수정했다. 시간 글자는 고정된 앞 레이어에 남고 주황 면만 접히도록 분리했다.
- 형광펜의 좌우 여백이 인라인 폭을 늘려 `...`과 글자 이동을 만들던 문제를
  수정했다. 글자 상자의 폭은 평상시와 같게 두고 주황 배경만 바깥으로 번지게 했다.
- 강조 묶음 앞 간격이 평상시 단위 간격보다 약 2px 좁고, 묶음 아래 여백 때문에
  글자가 약 1.5px 위로 뜨던 차이를 제거해 강조 전후 좌표를 동일하게 맞췄다.
- 주황 형광펜의 퇴장 동작을 제거했다. 변화 없는 조회에서는 최근 적립 결과의
  주황 강조를 그대로 유지하고, 적립 시간이 다시 달라질 때만 새 형광펜을 그린다.
- `1시간 59분 30초`처럼 긴 적립시간이 650px 폼에서 잘리지 않도록 팔로워 구역과
  적립 구역의 경계를 약 50px 왼쪽으로 옮겨 기본 팔로워 구역을 약 174px로 줄였다.
- 변화 없는 조회에서는 최근 적립시간의 주황 강조를 유지하고, 다음 적립 변동에만
  다시 그리는 동작과 174px 팔로워 구역을 `0.1.6` 배포 범위로 확정했다.
- 참고 이미지의 흰 외곽선과 어두운 반투명 면을 재해석한 `theme=glass` 시안을
  추가했다. 기본 종이 테마는 유지하며 흰 글자·유리 경계·붉은 적립 강조를 적용한다.
- 두 번째 참고 이미지처럼 배경 조명과 색이 유리판 아래에서 읽히도록 유리 테마의
  겹친 검은 배경을 단일 청회색 막으로 바꾸고 불투명도를 약 48~56%로 낮췄다.
- 유리 표면에 배경색을 흉내 낸 그라데이션이 남지 않도록 본체와 팔로워 구역의
  색 변화를 제거했다. 유리는 균일한 무채색 48% 막과 블러만 사용하며, 화면에
  보이는 색과 얼룩은 실제 위젯 뒤의 방송 장면에서만 들어온다.
- 편집기에 종이/유리 테마 선택기를 추가하고, 유리 테마 미리보기의 색은 iframe
  밖에 있는 별도 무대 배경에서만 나오도록 구성했다. 선택값은 미리보기와 OBS
  링크의 `theme` 파라미터에 즉시 반영하며 이 배포 버전을 `0.1.7`로 올렸다.
- 샘플의 다른 유리 패널과 같은 무채색 48% 막이 위젯을 처음 추가할 때부터
  적용되도록 기본 테마를 `glass`로 변경했다. `theme=paper`를 명시한 기존
  종이 테마 링크는 그대로 유지하며 이 배포 버전을 `0.1.8`로 올렸다.
- 첫 팔로워 값을 불러왔을 때도 현재 적립시간 전체에 주황 형광펜을 한 번
  그리도록 초기 상태를 강조 흐름에 포함했다. 유리 테마의 보조 문구를 완전
  불투명한 흰색으로 바꾸고 숫자 애니메이션의 문자 페이드는 제거했으며 이
  배포 버전을 `0.1.9`로 올렸다.
- 미리보기의 첫 장면은 로딩 연출이 아니라 이미 방송 중인 안정 상태이므로
  현재 적립시간의 형광펜을 완성된 채로 즉시 표시한다. 첫 장면에서는 애니메이션을
  재생하지 않고, 650ms 뒤 새 팔로워 이벤트가 발생할 때만 변경 단위를 다시 그리며
  이 배포 버전을 `0.1.10`으로 올렸다.
- 이 프로젝트는 변경 검증이 끝나면 별도 확인 없이 바로 GitHub Pages까지
  배포한다는 운영 규칙을 프로젝트 `AGENTS.md`에 기록했다.
- 증가 연출은 `원인 → 결과`가 두 번의 반대 방향 획으로 이어진다는 원칙을
  세웠다. 650px 폼에서는 증가량 카드의 면적 대신 궤적을 크게 쓰도록 작은
  `+30초` 티켓과 긴 주황 속도선이 오른쪽에서 빠르게 들어오고, 곧바로 적립
  숫자와 주황선이 왼쪽에서 오른쪽으로 채워진다. 티켓은 레이아웃 공간을
  예약하지 않으며 체류시간을 1.35초로 줄이고 적립 숫자의 순간 페이드를 복원해
  유리 테마 이전의 빠른 착지감을 되살렸다. 이 배포 버전을 `0.1.11`로 올렸다.
- 증가 티켓의 진입·퇴장 속도는 유지하면서 읽을 수 있는 가운데 정지 구간을 약
  1.25초로 늘렸다. 전체 노출은 2.15초이며, 긴 적립시간과 겹치지 않도록 티켓을
  높이 약 32px의 상단 한 줄 형태로 바꿨다. `1시간 59분 30초 → 2시간` 미리보기
  시나리오를 650×100에서 검증했다. 한 줄에 라벨과 증가량을 함께 넣으면
  `+30초`가 말줄임되는 문제를 발견해 라벨은 접근성 텍스트로 유지하고 증가량만
  크게 표시했으며, 이 배포 버전을 `0.1.12`로 올렸다.
- 실제 방송 화면에서는 부분 단위 강조보다 적립시간 전체 변화가 더 명확하다는
  피드백에 따라 시간·분·초 분리 강조를 제거했다. 직전 값과 다르면 현재 적립시간
  전체를 하나의 주황 배경으로 묶어 다시 긋고, 값이 같으면 기존 배경을 유지한다.
  이 배포 버전을 `0.1.13`으로 올렸다.
- 편집기에 `업보 타이머` 탭을 추가했다. 한국시간 시작 시각을 Unix timestamp로
  URL에 넣고, `예상 종료시각 = startAt + 누적 업보시간`을 단일 진실원으로 삼아
  OBS 비활성화·늦은 로딩·새로고침 뒤에도 남은시간을 복구한다. 게임별 최고
  팔로워와 종료 상태는 `session` 단위로 보존한다.
- 타이머 폼은 기존 174px 팔로워 구역과 650×100 유리 외곽을 유지한다. 오른쪽에
  큰 남은시간과 작은 종료시각을 함께 배치하고, `+30초` 티켓이 오른쪽에서 들어온
  뒤 주황 궤적이 남은시간을 올리고 다시 종료시각을 오른쪽으로 미는 릴레이
  애니메이션을 적용했다. 샘플은 `1시간 59분 30초 → 2시간`과 종료시각 30초
  연장을 재생하며 이 배포 버전을 `0.1.14`로 올렸다.
- 방송 표현을 `업보`로 통일했다. 업보 탭에서 기존 행동·남은시간·종료시각
  문구뿐 아니라 코드에 고정돼 있던 시작 전 상태와 0초 종료 문구까지 직접
  수정해 URL에 포함할 수 있게 했다. 업보 전용 입력란이 팔로우 적립 탭에
  노출되던 `hidden` 스타일 충돌도 바로잡았으며 이 배포 버전을 `0.1.15`로 올렸다.
- 배포 레포지토리에서 바뀐 업보 기본 문구(`업보`, `이대로면`, `업보 청산`)를
  편집기 HTML과 문서에도 동기화했다. 미리보기 재생 시 `about:blank`와
  `requestAnimationFrame`을 거치던 구조는 백그라운드 탭에서 재생 시작이
  지연되는 원인이었다. 매번 고유 URL로 iframe을 즉시 다시 불러오고 시작시각은
  iframe 로딩 시점에 정하도록 바꿔 샘플이 항상 `1시간 59분 30초 → 2시간`에서
  출발하게 했으며 이 배포 버전을 `0.1.16`으로 올렸다.
- `0.1.16` 미리보기는 iframe 로딩 순간의 초를 시작시각으로 사용해 종료시각이
  `:59 → :29`처럼 보일 수 있었다. 계산식에는 다른 항목이 없었지만 샘플의
  기준 초가 임의였던 문제다. 미리보기 시작시각을 현재 분의 `:00`으로 정렬해
  종료시각이 팔로워 한 명마다 정확히 `:00 ↔ :30`으로 이동하도록 고정하고,
  계산 회귀 테스트를 추가했다. 미리보기 가상 시계는 업보 시작 5초 전부터
  흐르고, 시작 0.65초 뒤 팔로워 증가 릴레이가 이어지도록 구성해 이 배포
  버전을 `0.1.17`로 올렸다.
- 업보 시작 직전의 상태를 `waiting → count-in → starting → running`으로
  명시했다. `T-3초`부터 `-3초/-2초/-1초`를 주황색으로 매초 다시 칠하고,
  `T=0`부터 편집 가능한 `링피트 시작`을 3초간 유지한 뒤 `T+3초`에 정상
  남은시간 타이머로 전환한다. `T+0.65초`의 팔로워 증가 릴레이는 시작 문구를
  가리지 않고 증가량과 종료시각만 먼저 갱신한다. 시작 전 갱신도 `startAt`
  기준의 정각 초 경계에 맞춰 OBS 로딩 시점과 무관하게 문구가 제때 바뀌도록
  했으며 이 배포 버전을 `0.1.18`로 올렸다.
- 업보 시작 예고를 `T-60초`부터 재생하고 상단 `준비중 >> 링피트`와 하단
  초 카운트다운을 분리했다. `T=0`의 `링피트 시작!!` 문구와 주황 배경은
  함께 3초간 고정하며, 시작 중 확인된 팔로워 증가는 정상 타이머가 나타난 뒤
  첫 예외 조회인 `T+5초`에 합쳐 보여준다. 이후 일반 조회는 `startAt` 기준
  `30·60·90초` 격자에 정확히 맞춘다.
- 종료 30초 안에서는 남은 `30·20·10·5·1·0초`에 추가 조회하고 매초 남은시간
  배경을 다시 칠한다. 0초에는 마지막 응답과 최소 3초를 기다리며, 남은 5초
  이하에서 팔로워가 늘면 `끝난줄?`과 `+30초` 연장 릴레이를 재생한다.
  편집기에 `시작 60초 보기`와 `종료 30초 보기`를 분리해 추가하고 이 배포
  버전을 `0.1.19`로 올렸다.
- 종료 장면의 기본 문구가 지나치게 위협적으로 보이지 않도록 `업보 청산`을
  `링피트 종료`로 바꿨다. URL의 `endedText` 사용자 지정 기능은 그대로
  유지하며 이 배포 버전을 `0.1.20`으로 올렸다.
- 시작·종료 연출 사이의 정상 진행 상태에서는 남은시간 뒤 주황 배경을 항상
  유지하도록 바꿨다. 1초 감소는 배경을 다시 재생하지 않고 숫자만 움직이며,
  팔로워 증가로 남은시간이 갱신될 때에만 기존 왼쪽→오른쪽 형광펜 연출을
  다시 실행한다. 진행 중 증가 연출의 배경 퇴장은 제거하고 종료 30초 연출은
  기존 동작을 유지했으며 이 배포 버전을 `0.1.21`로 올렸다.
- 방송 문구에서 부정적인 `업보` 표현을 제거하고 카운트다운 탭과 결과 라벨을
  `운동`으로 통일했다. T-60보다 이른 대기 상태는 기존 적립판과 이어지도록
  결과 라벨을 `적립`으로 유지하고 상단의 `시작 전 ·` 접두사를 제거하며,
  T=0 이후에만 결과 라벨을 `운동`으로 전환한다. 기존 URL의 `waitingText`
  파라미터는 파싱 호환만 유지하고 편집기에서는 제거했으며 이 배포 버전을
  `0.1.22`로 올렸다.
- T-60보다 이른 시작 대기 화면의 `적립 N시간…` 뒤에도 진행 상태와 같은
  주황 배경을 완성된 상태로 유지한다. 대기 중에는 배경을 반복 재생하지 않고,
  기존 T-60 시작 카운트다운부터의 연출은 그대로 유지했으며 이 배포 버전을
  `0.1.23`으로 올렸다.
- 최종 종료 문구를 `!!! 링피트 완주 !!!`로 바꿨다. 종료 상태에서는 상단
  행동 문구를 숨기고 오른쪽 영역을 `가운데 문구 | 종료시간` 두 칸으로 유지해,
  전체 화면이 `팔로워 구역 | 가운데 정렬 완주 문구 | 종료시간 구역`으로
  읽히도록 전용 정렬을 적용했다. 다른 상태의 배치는 유지하며 이 배포 버전을
  `0.1.24`로 올렸다.
- 편집기 첫 화면을 문구 입력판에서 버튼 선택 화면으로 재설계했다. 초심자가
  실제로 결정해야 하는 세 가지(위젯 종류 · 테마 · 운동 시작 시각)만 전면에
  두고, 문구 10칸과 크기·글자·계산 설정은 모두 접기로 내렸다. 테마 라벨은
  내부 용어인 `종이/유리` 대신 사용자가 쓰는 `흰색/투명`으로 바꾸고 스와치를
  카드 폭 전체로 키웠다. 시작 시각은 `datetime-local` 타이핑 대신 큰 한국어
  표기와 `오늘/내일/이번 주 일요일` 칩, `±10분/±1시간` 스테퍼로 조작하며
  정밀 지정이 필요할 때만 `직접 입력`을 펼친다. 기본값은 HTML에 하드코딩된
  과거 시각 대신 로드 시점 기준 다음 일요일 14:00(KST)으로 계산한다.
  초심자가 판단할 수 없던 `게임 구분 이름` 필드는 삭제하고 시작 시각에서
  `ringfit-YYYYMMDD-HHmm`으로 자동 생성하며, 같은 값을 두 번 노출하던
  `전체 글자` 숫자칸+슬라이더는 슬라이더 하나로 합쳤다. 강한 시각면은
  7개(hero 메모·탭 바·폼 레일·주황 점선 박스 포함)에서 3개(폼·미리보기·링크)로
  줄였다. 위젯 URL 파라미터는 하나도 바꾸지 않아 기존 OBS 링크는 그대로
  동작한다.
- 운동 시작 시각의 `직접 입력` 접기를 없애고 날짜·시각 입력칸을 항상 펼친
  상태로 뒀다. 접기 트리거가 12px 회색 시스템 폰트 한 줄이라 무엇을 눌러야
  직접 입력이 열리는지 알아볼 수 없었고, 버튼으로 못 맞추는 시각을 고치려면
  숨은 트리거를 먼저 찾아야 했다. 이제 칩·스테퍼 아래에 점선 구분선을 두고
  `날짜와 시각 직접 고치기` 라벨이 붙은 입력칸을 바로 노출한다. 입력칸은
  48px 높이에 등폭 글꼴을 써서 날짜 숫자가 또렷하게 읽히도록 했다. 값을
  직접 고치면 위쪽 큰 표기·세션 이름·링크가 그대로 따라 움직이고 지난
  시각이면 경고 배지가 뜬다. 이 배포 버전을 `0.1.26`으로 올렸다.
- 운동 타이머의 시작 운동시간을 기존 팔로워 적립량뿐 아니라 `1~1440분` 범위의
  직접 입력값으로도 정할 수 있게 했다. 직접 설정 모드는 첫 팔로워 조회값을
  연장 기준으로 잡아, 시작 시간은 고정하면서 이후 증가분만 1명당 적립시간만큼
  더한다. `팔로워 증가 시 시간 연장`을 끄면 팔로워 수는 계속 갱신하되 타이머와
  종료시각은 늘리지 않고, 기본 행동 문구도 연장 없음 상태에 맞춰 바뀐다.
  편집기의 새 버튼 중심 흐름에는 `몇 분 할까` 단계를 추가하고 연장 여부는 기존
  `팔로워 계산 설정` 안에 두었다. 이전 URL은 기본값이 팔로워 자동 산정·연장
  켜짐이라 같은 방식으로 동작하며 이 배포 버전을 `0.1.27`로 올렸다.
