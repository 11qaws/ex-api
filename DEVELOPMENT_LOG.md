# DEVELOPMENT LOG

## 2026-07-27

- `11qaws/ex-api` 독립 저장소와 로컬 격리 작업공간을 초기화했다.
- `rettohighlight`의 `uf-row`에서 왼쪽 정체성 레일, 유레카 테마 틴트, 오른쪽 초상
  bleed와 scrim 구조를 가져왔다.
- 초기 디자인 시안의 핵심인 현재 팔로워와 링피트 결과의 즉시 비교, 차가운 초록과
  뜨거운 주황의 대비를 유지했다.
- 기준 1,031명, 팔로워 1명당 0.5분 계산 모듈과 URL 설정을 구현했다.
- 공식 치지직 Open API의 비밀키를 숨기는 Cloudflare Worker 프록시를 구현했다.
- GitHub Pages 배포 워크플로와 운영 설정 문서를 추가했다.
- 1280×100 실제 렌더에서 초상 과확대를 발견하고, 이미지 크기와 눈 중심 초점을
  와이드 위젯 비율에 맞게 조정했다.
- 정갈한 UI 인상을 줄이고 스트리머의 투박한 표현 취향을 살리기 위해 제목·숫자를
  `Gasoek One`으로 교체하고, 비뚤어진 스탬프·잉크 망점·거친 티켓 그림자를 더했다.
- 컬러 초상을 흰색 러프 선화 자산으로 변환해 오른쪽 초록 배경에 스크린 프린트처럼
  배치했다. 중앙 메시지는 현황 설명 대신 `팔로우 누르면 +30초!` 행동 유도로 바꿨다.
- 반듯한 디스플레이체를 Nintendo DS풍 픽셀 글꼴 `Galmuri11 Bold`로 교체했다.
  떠 있던 결과 티켓은 전체 높이의 고정 주황 패널로 바꾸고 카피를 `도망못감;;`,
  `지금 +n명 쌓임`처럼 방송 채팅에 가까운 말투로 다듬었다.
- 폰트 교체만 반복하던 카드 구성을 폐기하고, 급하게 써 붙인 낙서 전단 콘셉트로
  전체 표현을 다시 설계했다. `팔로우 눌러서 나 운동시키기`를 주 문장으로 올리고
  `1명 = +30초 → 일요일 링피트 n분째ㅋㅋ`를 한 줄 결과 문장으로 통합했다.
- 최종 카피를 `팔로우 눌러서 일요일 링피트 적립 n분 (+30초)` 한 문장으로
  압축했다. `ㄱㄱ`, 참여 인원, 화살표, 부가 낙서를 삭제하고 왼쪽 기준 정보까지
  `Gaegu` 손글씨로 통일했다.
- 방송 화면에서 더 거칠고 즉각적으로 읽히도록 중앙 문장을 40px대로 키우고,
  결과 숫자는 1.3배로 돌출시켰다. 왼쪽 팔로워 메모도 모든 글자 크기를 함께 올렸다.
- 캐릭터 이미지와 수동 LIVE 버튼을 제거하고 동적 요소를 세 가지로 제한했다:
  API 팔로워 수, 기준일부터의 총 링피트 시간, 직전 조회 대비 신규 팔로워 이벤트.
  신규 인원은 30초 단위로 환산해 `+30초`, `+1분`, `+1분 30초`처럼 6초간 표시한다.
- LIVE 버튼 마크업 삭제 뒤 남은 `statusButton` 갱신 코드가 브라우저 실행을
  중단시키는 문제를 렌더 검수에서 발견했다. 잔여 참조를 제거하고 재검증했다.
- OBS 환경별 크기 조절을 위해 `width`, `height`, `fontSize` URL 설정을 추가했다.
  기본값은 1280×100·54px이며, 글자 크기에 맞춰 팔로워 영역과 신규 적립 이벤트
  폭도 함께 비례하도록 CSS 변수로 연결했다.
- 기본 OBS 캔버스를 800×150·48px로 변경했다. 좁아진 가로 폭에서도 세 동적
  요소가 유지되도록 소형 화면 보정 breakpoint를 640px 아래로 내렸다.
- 150px 높이가 방송 패널로는 과도해 기본 캔버스를 800×100으로 다시 낮췄다.
- 800×100에서 증가 이벤트와 총 적립 시간이 겹치지 않도록 문장을
  `팔로우 눌러서 일요일 / 링피트 적립 n분` 두 줄로 재배치했다.
- 800px 폭에서 `n분 30초`가 잘리지 않도록 문장을
  `팔로우 눌러서 일요일 링피트 / 적립 n분 30초`로 분할했다. 조건부 폰트 축소는
  제거하고 총 적립값의 큰 크기를 유지했다.
- 팔로워 증가 시 숫자가 위아래로 교대되고, 오른쪽 적립 스티커가 충돌하듯 들어온
  뒤 총 시간이 형광펜으로 다시 칠해지는 3단계 애니메이션을 연결했다. 평상시에는
  상태 점만 약하게 호흡하고, `prefers-reduced-motion`에서는 이동 효과를 제거한다.
- 애니메이션 종료 후 교체 전 팔로워 숫자 레이어가 다시 나타나 현재 숫자와 겹치던
  문제를 퇴장 레이어의 기본 투명 상태로 수정했다.
- 최초 배포 환경에 치지직 개발자 키가 없는 경우에도 실시간 조회가 가능하도록
  Worker에 치지직 공개 채널 응답 경로를 추가했다. `Client ID`와 `Client Secret`이
  모두 설정되면 기존 공식 Open API 경로를 자동으로 우선한다.
- 팔로워 교체 애니메이션이 `지금 팔로워` 라벨을 침범하지 않도록 숫자 전용
  클리핑 높이와 위아래 간격을 분리했다. 오른쪽 이벤트의 `방금 적립` 라벨은
  약 15px에서 20px로 키우고 패딩을 재배분해 100px 높이에서도 읽히게 했다.
- 외곽 폼, 왼쪽 정체성 띠, 팔로워 구역, 주황 이벤트 폼의 치수와 좌표를
  `docs/WIDGET_FORM_SPEC.md`에 고정했다. 반복 사용되는 치수는 CSS 변수로 추출해
  문서와 실제 구현이 같은 값을 참조하도록 정리했다.

## 2026-07-28

- 기존 OBS 위젯 주소의 하위 호환성을 유지하면서 `/editor/`에 전용 편집기를
  추가했다. 문구, 800×100 기준 폼 크기, 전체·위치별 글자 크기를 입력 즉시
  미리보기 iframe과 URL에 반영한다.
- 편집 상태를 `draft`, `previewUrl`, `widgetUrl`, `copyState`로 분리했다.
  애니메이션 확인용 `preview`, `eventDelta` 값이 실제 OBS 링크에 섞이지 않도록
  별도 URL을 생성한다.
- 전체 글자 크기를 움직이면 기존 디자인 비율에 맞춰 위치별 크기가 함께 바뀌며,
  고급 설정에서 각 위치를 다시 독립 조절할 수 있게 했다.
- 사용자 문구는 공백을 정규화하고 길이를 제한한 뒤 `textContent`로만 출력한다.
  `{initial}` 토큰은 포맷된 기준 팔로워 수로 안전하게 치환한다.
- 편집기 배색은 유레카 초록·주황으로 고정하고, 기본 사용 흐름 뒤에 위치별 폰트와
  계산 설정을 접어 두어 첫 사용자가 문구 수정부터 링크 복사까지 한 화면에서
  끝낼 수 있게 구성했다.
- 팔로워 계산 설정에서 새로고침 입력을 제거했다. 위젯의 기본 30초 갱신은 그대로
  유지하며, 편집기가 만드는 링크에도 불필요한 `refresh` 파라미터를 넣지 않는다.
- 편집기 안의 라이브 미리보기만 10초마다 갱신하도록 미리보기 URL에
  `refresh=10`을 적용했다. OBS용 생성 링크의 기본 30초 주기와는 분리된다.
- 도움말이 있는 기준 안내 필드가 같은 행의 다른 입력칸을 아래로 밀던 문제를
  수정했다. 모든 문구 필드를 위쪽 기준으로 고정해 라벨과 입력창 시작선을 맞췄다.
